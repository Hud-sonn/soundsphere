"""Nigerian event sources — tickethub.ng + padeya.com.

Both are Next.js apps whose listings are client-rendered, but they embed
complete event objects in React Flight data (probed 2026-09-15), so plain
HTTP + regex is enough. No browser automation needed.
"""
